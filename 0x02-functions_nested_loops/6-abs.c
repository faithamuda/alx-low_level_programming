#include "main.h"

/**
 * _abs - comput3s the absolute value of an integer
 * @int: integer argument 
 *
 * Return : absolute value of integer
 */

int _abs(int c)
{
	if (c >= 0)
		return (c);
	else
		return (0 - c);
}
